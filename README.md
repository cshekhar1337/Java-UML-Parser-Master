# Java-UML-Parser-Master

This application helps in making UML diagram from given code. It looks for all the java file in the directory and generates a uml diagram
It uses plantuml to create the images.
